<ResourceDictionary x:Class="UI.Common.Resources.Dictionaries.MainResourceDictionary"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:system="clr-namespace:System;assembly=mscorlib"
                    xmlns:textBoxes="clr-namespace:UI.Controls.TextBoxes">

    <SolidColorBrush x:Key="green_brush" Color="#2FC97B"/>
    <SolidColorBrush x:Key="green_highlight_brush" Color="#39D385"/>
    <SolidColorBrush x:Key="positive_hightlight_brush" Color="#1FFF1F"/>
    <SolidColorBrush x:Key="warning_yellow_brush" Color="#FFDC00"/>
    <SolidColorBrush x:Key="error_red_brush" Color="#F41312"/>
    <SolidColorBrush x:Key="salmon_brush" Color="#E2464A"/>
    <SolidColorBrush x:Key="salmon_highlight_brush" Color="#EC5054"/>

    <DropShadowEffect x:Key="window_drop_shadow" ShadowDepth="5"/>

    <system:Double x:Key="font_size">14</system:Double>
    <system:String x:Key="application_name">Asset Ops</system:String>

    <BitmapImage x:Key="application_logo_icon"
                 UriSource="\Common\Resources\Assets\Icons\app_logo_icon.png"/>
    <BitmapImage x:Key="english_icon"
                 UriSource="\Common\Resources\Assets\Icons\english_icon.png"/>
    <BitmapImage x:Key="chinese_icon"
                 UriSource="\Common\Resources\Assets\Icons\chinese_icon.png"/>
    <BitmapImage x:Key="russian_icon"
                 UriSource="\Common\Resources\Assets\Icons\russian_icon.png"/>
    <BitmapImage x:Key="german_icon"
                 UriSource="\Common\Resources\Assets\Icons\german_icon.png"/>
    <BitmapImage x:Key="italian_icon"
                 UriSource="\Common\Resources\Assets\Icons\italian_icon.png"/>
    <BitmapImage x:Key="spanish_icon"
                 UriSource="\Common\Resources\Assets\Icons\spanish_icon.png"/>
    <BitmapImage x:Key="portuguese_icon"
                 UriSource="\Common\Resources\Assets\Icons\portuguese_icon.png"/>
    <BitmapImage x:Key="french_icon"
                 UriSource="\Common\Resources\Assets\Icons\french_icon.png"/>
    <BitmapImage x:Key="japanese_icon"
                 UriSource="\Common\Resources\Assets\Icons\japanese_icon.png"/>

    <Style x:Key="base_text_box_style" TargetType="{x:Type TextBox}">
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="CaretBrush" Value="{DynamicResource theme_place_holder_brush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource theme_font_brush}"/>
        <Setter Property="FontSize" Value="{DynamicResource font_size}"/>
        <Setter Property="FontFamily" Value="Calibri"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource theme_place_holder_brush}"/>
    </Style>

    <FrameworkElement x:Key="grab_cursor_dummy" Cursor="..\Assets\Cursors\grab.cur"/>
    <FrameworkElement x:Key="grabbing_cursor_dummy" Cursor="..\Assets\Cursors\grabbing.cur"/>

    <Style x:Key="base_textblock_style" TargetType="{x:Type TextBlock}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontFamily" Value="Calibri"/>
        <Setter Property="FontSize" Value="{DynamicResource font_size}"/>
        <Setter Property="Foreground" Value="{DynamicResource theme_font_brush}"/>
        <Setter Property="TextAlignment" Value="Center"/>
    </Style>

    <Style x:Key="base_buttons_style" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource theme_font_brush}"/>
        <Setter Property="FontFamily" Value="Calibri"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="FontSize" Value="{DynamicResource font_size}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    </Style>

    <Style x:Key="base_listview_style" TargetType="{x:Type ListView}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="IsSynchronizedWithCurrentItem" Value="True"/>
        <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel"/>
    </Style>

    <Style x:Key="icon_buttons_style" 
           TargetType="{x:Type Button}"
           BasedOn="{StaticResource base_buttons_style}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="highlight"
                            Opacity="{TemplateBinding Opacity}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="3">
                        <ContentPresenter/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="highlight"
                                    Property="Background"
                                    Value="{DynamicResource theme_button_highlight_brush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="highlight"
                                    Property="Background"
                                    Value="{DynamicResource theme_button_on_click_brush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="text_buttons_style" 
           TargetType="{x:Type Button}"
           BasedOn="{StaticResource base_buttons_style}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="highlight"
                            Opacity="{TemplateBinding Opacity}"
                            Background="{TemplateBinding Background}"
                            BorderThickness="1"
                            CornerRadius="3">
                        <TextBlock Text="{Binding Path=Content, RelativeSource={RelativeSource TemplatedParent}}" 
                                   Style="{StaticResource base_textblock_style}"
                                   TextAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="{Binding Path=FontSize, RelativeSource={RelativeSource TemplatedParent}}">
                        </TextBlock>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="highlight"
                                    Property="Background"
                                    Value="{StaticResource green_brush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="highlight"
                                    Property="Background"
                                    Value="{DynamicResource green_highlight_brush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
        <Setter Property="Margin" Value="0,0,3,0"/>
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="IsTabStop" Value="false"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border x:Name="thumb"
                            SnapsToDevicePixels="True"
                            Background="{StaticResource green_brush}"
                            CornerRadius="3"
                            Height="{TemplateBinding Height}"
                            Width="{TemplateBinding Width}"/>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="thumb" 
                                    Property="Background" 
                                    Value="{StaticResource green_highlight_brush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel"/>
        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
        <Setter Property="Width" Value="10"/>
        <Setter Property="MinWidth" Value="{Binding Width, RelativeSource={RelativeSource Self}}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid x:Name="Bg" 
                          SnapsToDevicePixels="true" 
                          Background="Transparent">
                        <Border>
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="true"
                                   IsEnabled="{TemplateBinding IsMouseOver}"
                                   Width="10"
                                   HorizontalAlignment="Center">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Opacity="0" Command="{x:Static ScrollBar.PageUpCommand}" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Opacity="0" Command="{x:Static ScrollBar.PageDownCommand}" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumb}" />
                                </Track.Thumb>
                            </Track>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto"/>
                <Setter Property="MinWidth" Value="0"/>
                <Setter Property="Height" Value="14"/>
                <Setter Property="MinHeight" Value="{Binding Height, RelativeSource={RelativeSource Self}}"/>
                <Setter Property="Margin" Value="0 -14 0 0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid x:Name="Bg" SnapsToDevicePixels="true">
                                <Track x:Name="part_Track"
                                       IsEnabled="{TemplateBinding IsMouseOver}"
                                       Height="6">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Opacity="0" 
                                                      Command="{x:Static ScrollBar.PageLeftCommand}"/>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Opacity="0" 
                                                      Command="{x:Static ScrollBar.PageRightCommand}"/>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb x:Name="part_thumb" Style="{StaticResource ScrollBarThumb}" />
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type ScrollViewer}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid x:Name="Grid" 
                          Background="{TemplateBinding Background}">
                        
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Rectangle x:Name="Corner" 
                                   Grid.Column="1" 
                                   Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" 
                                   Grid.Row="1"/>
                        <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" 
                                                CanContentScroll="{TemplateBinding CanContentScroll}" 
                                                CanHorizontallyScroll="False" 
                                                CanVerticallyScroll="False" 
                                                ContentTemplate="{TemplateBinding ContentTemplate}" 
                                                Content="{TemplateBinding Content}" 
                                                Grid.Column="0" 
                                                Margin="{TemplateBinding Padding}" 
                                                Grid.Row="0"/>
                        
                        <ScrollBar x:Name="PART_VerticalScrollBar" 
                                   Opacity="0.2"
                                   AutomationProperties.AutomationId="VerticalScrollBar" 
                                   Cursor="Arrow" 
                                   Grid.Column="1"
                                   Maximum="{TemplateBinding ScrollableHeight}" 
                                   Minimum="0" 
                                   Grid.Row="0" 
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                   Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"/>
                        
                        <ScrollBar x:Name="PART_HorizontalScrollBar"
                                   Opacity="0.2"
                                   AutomationProperties.AutomationId="HorizontalScrollBar"
                                   Cursor="Arrow" 
                                   Grid.Column="0"
                                   Maximum="{TemplateBinding ScrollableWidth}"
                                   Minimum="0" 
                                   Orientation="Horizontal"
                                   Grid.Row="1" 
                                   Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                   Value="{Binding HorizontalOffset, Mode=OneWay, 
                                           RelativeSource={RelativeSource TemplatedParent}}" 
                                   ViewportSize="{TemplateBinding ViewportWidth}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation To="1" Duration="0:0:0.3" Storyboard.TargetName="PART_VerticalScrollBar" Storyboard.TargetProperty="Opacity" />
                                    <DoubleAnimation To="1" Duration="0:0:0.3" Storyboard.TargetName="PART_HorizontalScrollBar" Storyboard.TargetProperty="Opacity" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation To="0.2" Duration="0:0:0.3" Storyboard.TargetName="PART_VerticalScrollBar" Storyboard.TargetProperty="Opacity" />
                                    <DoubleAnimation To="0.2" Duration="0:0:0.3" Storyboard.TargetName="PART_HorizontalScrollBar" Storyboard.TargetProperty="Opacity" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type textBoxes:PlaceHolderTextBox}" 
           BasedOn="{StaticResource base_text_box_style}">
        <Setter Property="Padding" Value="5,0,0,0"/>
        <Setter Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource Self}, Path=Background}"/>
        <Setter Property="TextWrapping" Value="NoWrap"/>
        <Setter Property="Tag" Value="{DynamicResource theme_place_holder_brush}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type textBoxes:PlaceHolderTextBox}">
                    <ControlTemplate.Resources>
                        <BooleanToVisibilityConverter x:Key="boolean_to_visibility_converter"/>
                    </ControlTemplate.Resources>

                    <Grid MinHeight="32">

                        <Border x:Name="border" 
                                BorderBrush="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Tag}" 
                                BorderThickness="1"
                                CornerRadius="2"
                                Background="{DynamicResource theme_control_wrapper_background_brush}"
                                SnapsToDevicePixels="True">

                            <TextBlock x:Name="place_holder_text"
                                       Style="{StaticResource base_textblock_style}"
                                       Foreground="{DynamicResource theme_place_holder_brush}"
                                       Text="{TemplateBinding PlaceHolder}"
                                       TextAlignment="Left"
                                       FontStyle="Italic"
                                       VerticalAlignment="Center"
                                       Visibility="{TemplateBinding IsEmpty, 
                                                    Converter={StaticResource boolean_to_visibility_converter}}"
                                       Padding="5, 0, 0, 0"/>
                        </Border>

                        <ScrollViewer x:Name="PART_ContentHost" 
                                      Focusable="false"
                                      VerticalAlignment="Center"
                                      HorizontalScrollBarVisibility="Hidden" 
                                      VerticalScrollBarVisibility="Hidden"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border"
                                    Property="BorderThickness"
                                    Value="2"/>
                            <Setter TargetName="border"
                                    Property="BorderBrush"
                                    Value="{DynamicResource theme_background_inverse_brush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="False">
                            <Setter TargetName="place_holder_text" 
                                    Property="FontStyle" 
                                    Value="Italic"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="place_holder_text" 
                                    Property="FontStyle" 
                                    Value="Normal"/>
                        </Trigger>
                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="control_wrapper_border_style" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{DynamicResource theme_control_wrapper_background_brush}"/>
        <Setter Property="CornerRadius" Value="3"/>
    </Style>

    <Style x:Key="expander_option_list_items_style" 
           TargetType="{x:Type ListView}"
           BasedOn="{StaticResource base_listview_style}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="{x:Type ListViewItem}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                <ContentPresenter Margin="1,0,1,0"/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Button Content="{Binding}"
                            Style="{StaticResource text_buttons_style}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="vector_down_icon_style" TargetType="{x:Type Polygon}">
        <Setter Property="Points" Value="1,6,11,6,6,11"/>
        <Setter Property="Fill" Value="{StaticResource green_brush}"/>
    </Style>

    <Style x:Key="vector_right_icon_style" TargetType="{x:Type Polygon}">
        <Setter Property="Points" Value="0,9,0,-1,6,4"/>
        <Setter Property="Fill" Value="{StaticResource green_brush}"/>
    </Style>
</ResourceDictionary>