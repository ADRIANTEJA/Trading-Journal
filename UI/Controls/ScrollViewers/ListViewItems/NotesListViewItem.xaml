<Border x:Class="UI.Controls.ScrollViewers.ListViewItems.NotesListViewItem"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:UI.Controls.ScrollViewers.ListViewItems"
        mc:Ignorable="d" 
        Height="80" 
        d:DesignWidth="300"
        BorderBrush="{DynamicResource green_brush}"
        BorderThickness="1"
        CornerRadius="3" 
        Margin="0,10,10,0">
    
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="{Binding Path=Title}"
                   Style="{StaticResource base_textblock_style}"
                   VerticalAlignment="Center"/>
    </Grid>
</Border>
