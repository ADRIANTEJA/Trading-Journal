<Button x:Class="UI.Controls.Buttons.MainWindow.ResizeButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="32" 
             d:DesignWidth="32">
    
    <Button.Template>
        <ControlTemplate>
            <Grid x:Name="button_template"
                  Background="{DynamicResource theme_background_brush}">

                <Image x:Name="resize_icon"
                       Source="{DynamicResource resize_button_icon}" 
                       Margin="10,10,10,10"/>
                <Image x:Name="resize_fullscreen_icon"
                       Source="{DynamicResource resize_fullscreen_button_icon}"
                       Visibility="Hidden"
                       Margin="10,10,10,10"/>
            </Grid>

            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter TargetName="button_template"
                            Property="Background"
                            Value="{DynamicResource theme_button_highlight_brush}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Button.Template>
</Button>
